const mongoose = require("mongoose");
const User = mongoose.model("users");
const moment = require("moment");

let setNewsObject = async news => {
  
  return news;
};


module.exports = setNewsObject;
